# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# --- 编辑器 / 系统 ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# --- 本机配置（每台机器不同，不入库）---
python-path.txt
# 每台机器不同的偏好设置，不入库
excel2sql.ini
.venv/

# --- 本项目产物 ---
# 生成的 SQL 与中间结果不入库（可复现即可）
*_hardcode.sql
out.sql
excel2sql-out/

# --- 私有测试资产（VM 端到端实测，不入库）---
# 夹具、跑批脚本、用例日志与库侧读回结果都放这里；内容随时可重生成，不随仓库分发
数据库测试/

# --- 本地素材（体积大，不入库；放到 示例数据/ 自行取用）---
示例数据/
*.xlsx
*.xls
!examples/*.csv
~$*
